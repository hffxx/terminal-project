import { SPEObject } from "@splinetool/runtime";
import { RefObject } from "react";

export const keyboardMove = (
  ref: RefObject<SPEObject>,
  num: number,
  axis: string
) => {
  const keyboard = ref.current;
  if (keyboard) {
  }
};
